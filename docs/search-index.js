window.studioDocs = [{"title":"Editing API","href":"api--index.html","text":" Editing API The editing API is shared by Lua, the command line, MCP, and the web studio. It works on map JSON without an engine, graphics window, filesystem, or network connection. Each request contains an API version and an ordered batch. Room and object operations use explicit IDs. A successful batch returns one complete document; a failed or canceled batch returns no edited document. The web studio commits a successful batch as one undoable edit. json { \"apiVersion\": 1, \"operations\": [ { \"op\": \"room.add\", \"id\": \"east\", \"name\": \"East Hall\", \"x\": 40, \"y\": 0, \"width\": 24, \"height\": 12 }, { \"op\": \"tiles.rectangle\", \"roomId\": \"east\", \"layer\": \"foreground\", \"materialId\": \"terrain\", \"x\": 0, \"y\": 0, \"width\": 24, \"height\": 2 }, { \"op\": \"camera.set\", \"ppu\": 16, \"width\": 320, \"height\": 180 } ] } Use [bounded tile queries](queries.md) to inspect existing terrain without transferring a full map. Use [the operation reference](operations.md) for fields and behavior. [The JSON Schema](automation.schema.json) describes the complete request envelope. AutomationEngine.Describe() returns the same schema with operation descriptions, coordinates, capabilities, and limits. Calling from .NET Reference metroidvania-studio/automation/MetroidvaniaStudio.Automation.csproj. csharp using System.Text.Json; using MetroidvaniaStudio.Automation; using var request = JsonDocument.Parse(requestJson); AutomationResult result = AutomationEngine.Apply(mapJson, request.RootElement, cancellationToken); string updatedMap = result.DocumentJson; JsonElement inspection = AutomationEngine.Inspect(updatedMap, cancellationToken); JsonElement capabilities = AutomationEngine.Describe(); AutomationResult contains: Field Meaning --- --- DocumentJson Validated map JSON in format version 2. CreatedIds Created room and object IDs, in creation order. OperationCount Number of operations processed, including valid no-ops. Changed Whether the normalized document changed. Apply never mutates the caller's string or stores a global document. Callers decide when to save or commit the result. Concurrent editors must compare the original document revision before committing; a pure batch does not resolve competing writes by itself. Coordinates and resources Room x and y are world tile coordinates. Tiles use integer coordinates relative to their room's bottom-left corner. Positive Y points upward. Object coordinates and dimensions are local tile units and can contain fractions. Source tiles remain 16 by 16 pixels; camera PPU and reference resolution are separate document settings. Use resource catalog IDs for materialId and definitionId. The API stores these IDs and never downloads or opens resource files. It does not create textures or infer engine asset paths. A host or engine adapter resolves the IDs against its catalog. Validation and compatibility API version 1 and map format version 2 are separate contracts. Existing format version 1 maps use the core's migration to version 2. Custom data belongs in the document, room, object, or styleground properties lists. Unrelated properties, stylegrounds, groups, and supported resource IDs are retained. Unknown JSON fields are rejected rather than silently discarded, matching the map loader. IDs are case-sensitive and globally unique across rooms, objects, groups, and stylegrounds. Explicit IDs make generated content repeatable. Omitted creation IDs receive generated values. Room duplication derives copied object IDs from the explicit new room ID and source object IDs, so repeating an identical batch against identical input produces identical copies. Locked rooms reject edits. Locked groups include their locked ancestors; painting, erasing, object changes, room deletion, resizing, and rotation cannot modify their contents. Room movement preserves local contents and can move an unlocked room containing locked groups. Automation does not expose an operation to unlock content. Malformed requests throw ArgumentException, with the zero-based operation index for operation failures. Invalid document data throws InvalidDataException. Cancellation throws OperationCanceledException. No partial result is returned for these failures. Limits Limit Maximum --- --- Request JSON 8 MiB UTF-8 Input or output map JSON 32 MiB UTF-8 Operations per batch 1,024 Visited tile cells per batch 1,048,576 Work units per batch 8,388,608 Rooms per document 1,024 Room width or height 1,024 tiles Objects per room 16,384 Nodes per object 4,096 Work includes indexed tile changes, hierarchy and object lookups, property merging, and the document snapshots required by room transformations. Large repeated room transformations can hit the work limit before the operation limit. Split such work into smaller batches and inspect each result. Tile writes share a cached dictionary for each room/layer throughout the batch, avoiding a scan of all existing tiles for every new cell. Cancellation is checked before and after document processing and during bounded edit loops. Parsing, core validation, and an individual core room transformation are synchronous; hosts that need a strict deadline should run work outside the UI thread and enforce their own timeout. Inspection Inspect returns apiVersion, formatVersion, tileSize, name, camera settings, document properties, and room summaries. A room summary contains roomId, name, bounds, visibility, lock state, foreground/background counts, properties, and object summaries with objectId and definitionId. The minimap object contains world tile bounds and shared room-edge connections. Each connection has roomAId, roomBId, vertical, coordinate, start, and end. truncated reports whether the connection limit was reached. These are layout connections; the renderer determines the visible openings and outline styling. Inspection does not change selection or move the editor camera. "},{"title":"Live web API","href":"api--live-api.html","text":" Live web API The web server exposes automation under /api/v1 on its existing loopback address. Use the address printed by the launcher; do not assume the default port is free. Download the [OpenAPI description](http.openapi.json) for HTTP endpoint names and request fields. For most clients, [CLI live mode](../cli/commands.md) or [MCP live mode](../mcp/setup.md) handles this protocol for you. Inspect and submit 1. GET /api/v1/capabilities returns the operation schema and execution limits. 2. GET /api/v1/document returns apiVersion, instanceId, documentRevision, and the active document. 3. POST /api/v1/jobs with Content-Type: application/json submits a Lua or batch job. 4. Poll GET /api/v1/jobs/{id} until its phase is terminal. Example batch request, after substituting the identity and revision from step 2: json { \"kind\": \"batch\", \"clientId\": \"my-local-client\", \"commandId\": \"a-unique-command-id\", \"expectedInstanceId\": \"instance-from-document-response\", \"expectedDocumentRevision\": 0, \"dryRun\": false, \"batch\": { \"apiVersion\": 1, \"operations\": [ { \"op\": \"document.update\", \"name\": \"New world\" } ] } } For Lua, replace batch with source, set kind to \"lua\", and optionally supply an integer seed (default 1). Job envelope fields are case-sensitive. Unknown or repeated fields are rejected, including misspelled dry-run flags. Submission returns HTTP 202 with a job object and its status URL in Location. Repeating the identical request with the same clientId and commandId returns the existing job while it is retained. Reusing that identity with different content is a conflict. Clients must re-inspect after completion; document revisions are not file hashes or durable identifiers. A lost submission can also be recovered without creating a new job using GET /api/v1/jobs/lookup?clientId=...&commandId=.... It returns the retained status or HTTP 404. This is useful before cancelling an uncertain submission. Status and cancellation A status contains id, phase, dryRun, operationCount, changed, logs, error, errorCode, and baseDocumentRevision. Phase Meaning --- --- running The isolated worker is executing completed The result was validated and applied, or validated as a dry run conflict The active document changed; nothing was applied cancelled Cancellation won before commit; nothing was applied failed Validation, execution, or a resource limit failed; nothing was applied Cancel with POST /api/v1/jobs/{id}/cancel and {\"clientId\":\"my-local-client\"}. Only the submitting client identity can cancel its job. A job already committed stays completed; use Undo in the editor if needed. Jobs run outside the painting lock. The final result is applied atomically as one edit only if the same document, instance, and revision are still current. An active painting gesture blocks submission. Dry runs never change the document or its history. Bounds and lifetime The host accepts at most 2 active jobs and retains the latest 32 job records in memory. Records and deduplication identities expire when evicted or when the server restarts. They are not a durable job queue. Lua source is limited to 64 KiB and HTTP batches to 8 MiB. Workers have a 15-second timeout, a 256 MiB managed heap setting, and a sampled 512 MiB process memory limit. Output is bounded. Cancellation and shutdown terminate only the worker owned by that host. The Lua runtime also limits instructions, operations, allocations, and logs; see [execution limits](../scripting/execution-limits.md). These are resource controls, not an operating-system sandbox for running arbitrary native code. The API keeps the existing Host/Origin checks and JSON request requirement. It is a local desktop interface, not an authenticated remote service. Client IDs support cancellation ownership and retry handling; they are not credentials. Do not forward its port or expose it through a proxy. Script library - GET /api/v1/scripts lists workspace scripts and read-only examples. - GET /api/v1/scripts/source?id=workspace/example.lua returns source and a revision hash. Use the id from the list response. - POST /api/v1/scripts saves { \"name\": \"example.lua\", \"source\": \"print(1)\", \"expectedRevision\": null } as a new workspace script. To update an existing file, provide the revision returned when it was opened. A conflicting save returns HTTP 409. Library requests never run scripts or change the active map. Files stay in the workspace Scripts/ folder. Names cannot contain directory separators or traversal. Symlinks and junctions are rejected. "},{"title":"Operation reference","href":"api--operations.html","text":" Operation reference All operations are objects with an op string. Required fields appear first below. Unknown fields, duplicate fields, incorrect types, and unsupported layer/shape names fail the batch. Optional fields keep their current value unless a default is listed. Rooms Operation Required fields Optional fields --- --- --- room.add x, y, width, height id, name room.update roomId name, visible room.move roomId, x, y — room.resize roomId, width, height x, y, crop: false, keepLocalContents: false room.rotate roomId clockwise: true room.duplicate roomId, x, y id, name room.delete roomId — room.add, room.move, and room.duplicate resolve an overlapping destination against existing rooms. They place the moved or created room at a free boundary and keep other rooms fixed. Inspect the returned document to obtain its final coordinates. room.resize uses the same contact layout as manual editing: attached rooms can move with the changed edge, while rooms separated by a gap stay fixed. A new overlap with a detached room rejects the operation. Without crop, the bounds stop at existing terrain. With crop, out-of-bounds target-room terrain is removed. By default, existing contents keep their world position when the origin changes; keepLocalContents: true keeps their local coordinates instead. This matches the editor's size fields. Existing object and node positions follow that resize behavior; crop removes terrain only. room.rotate turns by 90 degrees, transforms tile slopes, objects, and nodes, and uses the contact layout if width and height change. room.duplicate copies all supported room content and custom properties with fresh identities. room.delete also removes its tiles and objects. Tiles Tile layers are foreground and background. Shapes are solid, bottomLeft, bottomRight, topLeft, and topRight; a triangular shape is named after its filled right-angle corner. Operation Required fields Optional fields --- --- --- tiles.paint roomId, layer, cells materialId: \"terrain\", groupId: \"\", shape: \"solid\" tiles.erase roomId, layer, cells groupId: \"\" tiles.rectangle roomId, layer, x, y, width, height materialId: \"terrain\", groupId: \"\", shape: \"solid\", erase: false tiles.fill roomId, layer, x, y materialId: \"terrain\", groupId: \"\", shape: \"solid\", erase: false cells is an array of { \"x\": 0, \"y\": 0 } points. A point can also provide its own shape, overriding the operation's shape. Repeated coordinates are allowed; the last write wins. All coordinates and full rectangles must fit inside the room. Erasing a missing cell is a no-op. json { \"apiVersion\": 1, \"operations\": [ { \"op\": \"tiles.paint\", \"roomId\": \"east\", \"layer\": \"foreground\", \"materialId\": \"terrain\", \"cells\": [ { \"x\": 2, \"y\": 2, \"shape\": \"bottomRight\" }, { \"x\": 3, \"y\": 2 }, { \"x\": 4, \"y\": 2 } ] } ] } tiles.fill visits four-connected cells matching the starting cell's material, shape, and group. Empty cells match other empty cells. A different material, shape, or group forms a barrier. Group locks apply both to the destination group and to any existing cell that would be replaced or erased. Objects Object layers are entities, triggers, foregroundDecals, and backgroundDecals. Operation Required fields Optional fields --- --- --- object.add roomId, definitionId, layer, x, y id, groupId: \"\", width: 1, height: 1, rotation: 0, scaleX: 1, scaleY: 1, nodes, properties object.update roomId, objectId definitionId, groupId, x, y, width, height, rotation, scaleX, scaleY, nodes, properties object.delete roomId, objectId — Dimensions must be positive, scales must be nonzero, and all numeric values must be finite. Added or updated objects, including their rotated/scaled bounds and nodes, must fit within their room. nodes replaces the node array and contains { \"x\": 0, \"y\": 0 } points. Updating position alone does not offset an explicitly stored node path; provide the intended nodes when moving that path. properties merges string values by key; a null value removes a key. Layer changes are not supported by object.update; recreate the object in the required layer. Definition-specific rendering and placement restrictions belong to the host's catalog validation. Properties and camera Operation Required fields Optional fields --- --- --- properties.set values roomId document.update name — camera.set ppu, width, height — properties.set targets the document when roomId is absent. values is an object of string or null values. Existing unrelated keys remain unchanged. json { \"apiVersion\": 1, \"operations\": [ { \"op\": \"properties.set\", \"roomId\": \"east\", \"values\": { \"region\": \"forest\", \"music\": \"ambient\", \"obsolete\": null } }, { \"op\": \"camera.set\", \"ppu\": 16, \"width\": 320, \"height\": 180 } ] } Camera PPU must be an integer from 1 to 8,192. Reference width and height must be integers from 1 to 16,384. These settings are persisted through the existing camera property keys and do not resize source tile textures. "},{"title":"Tile queries","href":"api--queries.html","text":" Tile queries Read a bounded rectangle of existing terrain before preparing an edit. Tile queries never alter the map or its revision. sh metroidvania-studio query-tiles --workspace workspace --map maps/world.json --room hall --layer foreground --x 0 --y 0 --width 24 --height 2 The CLI result contains the map revision and a query object. studio_query_tiles provides the same query through MCP. Connected mode accepts --url and --map @active. json { \"roomId\": \"hall\", \"layer\": \"foreground\", \"x\": 0, \"y\": 0, \"width\": 2, \"height\": 1, \"cells\": [ { \"x\": 0, \"y\": 0, \"shape\": \"solid\", \"materialId\": \"terrain\", \"groupId\": \"\" } ] } Coordinates are local to the room. The rectangle must be inside the room, with positive dimensions and an area no larger than 4096 tiles. Supported layers are foreground and background. Empty coordinates are omitted. Results are ordered by Y, then X; cell coordinates remain room-local rather than relative to the requested rectangle. C callers can use AutomationEngine.QueryTiles(documentJson, roomId, layer, x, y, width, height, cancellationToken). For repeated queries, create an immutable snapshot with AutomationEngine.CreateTileQuery(documentJson, cancellationToken) and call its Query method. It reuses per-layer indexes and returns fresh JSON values. Later edits do not change that snapshot. Lua exposes this through studio.tiles.get { roomId = \"hall\", layer = \"foreground\", x = 0, y = 0, width = 2, height = 1 }. Queries observe the input map snapshot. Operations queued by the current script become visible only after the script commits successfully. "},{"title":"Building from source","href":"building.html","text":" Building from source Install Node.js 24+, .NET SDK 10, and Git. Run the build script for your platform: Platform Build Run --- --- --- Windows platform/win/web/build.bat platform/win/web/run.bat macOS platform/mac/build.command platform/mac/run.command Linux platform/linux/build.sh platform/linux/run.sh The scripts work from any current directory. The first build compiles the application and prepares its dependencies. Later builds compare source contents with the last successful bundle. If nothing changed, they reuse that bundle immediately. Otherwise, changed .NET projects compile incrementally, and a new complete bundle is prepared under builds/. The run script performs the same source check before starting. Saving map JSON, editing workspace textures, or changing file timestamps alone does not rebuild application code. A failed or interrupted build leaves the previous successful bundle available. Force a full rebuild Use this after manually changing generated build files or when you want to discard compiler reuse: text platform/win/web/build.bat --rebuild On macOS or Linux, pass --rebuild to the corresponding build script. PowerShell also accepts -Rebuild. Prebuilt release downloads use their included application files and do not compile source when started. "},{"title":"CLI commands","href":"cli--commands.html","text":" CLI commands Offline filesystem commands require --workspace DIRECTORY. The directory must already exist. Source and output paths are relative to this root. Commands and options may appear in either order. Command Required options Optional options Result --- --- --- --- help None None Command summary version None None Tool, API, and map format versions capabilities None None Complete operation schemas and limits init (create) --map --name, --dry-run New empty map inspect --map None Revision, camera, rooms, objects, region properties, shared room boundaries query-tiles --map, --room, --layer --x, --y, --width, --height Occupied cells in a bounded rectangle validate --map None Map contract validation apply --map, --batch --expected-revision, --dry-run One atomic operation batch run --map, --script --seed, --expected-revision, --dry-run One atomic Lua edit export-room --map, --room, --output --force, --dry-run One independent room JSON preview --map --output, --force, --dry-run SVG layout, inline when no output is given mcp (serve) None --read-only Local stdio MCP server --url http://127.0.0.1:PORT connects inspect, query-tiles, validate, apply, run, preview, and mcp to a running web editor. These commands use --map @active. CLI batch/script files still require an explicit --workspace; MCP accepts source/batches inline and needs no file root in connected mode. Live previews return SVG in JSON. init and export-room use offline mode. Localhost aliases are resolved to the loopback address directly; proxies, redirects, credentials, and nonlocal endpoints are rejected. --read-only is accepted for every filesystem command. Existing map edits through apply and run are explicit in-place updates; --force is only for replacing an export or preview output. Outputs cannot replace their source map. A dry run returns proposed metadata without changing files. Export dry runs validate paths and conflicts without creating their output. A batch JSON file contains an API envelope: json { \"apiVersion\": 1, \"operations\": [ { \"op\": \"room.add\", \"id\": \"hall\", \"name\": \"Hall\", \"x\": 0, \"y\": 0, \"width\": 24, \"height\": 12 }, { \"op\": \"tiles.rectangle\", \"roomId\": \"hall\", \"layer\": \"foreground\", \"x\": 0, \"y\": 0, \"width\": 24, \"height\": 2 } ] } Use stable room IDs instead of names or UI selection. Coordinates use tile units with positive Y pointing up. The API rejects unknown operation fields and unsupported versions. Get exact schemas with capabilities. Successful results use camelCase JSON. File revisions are lowercase SHA-256 hashes of the file bytes. A changed file produces a new revision. --dry-run leaves the on-disk revision unchanged and returns a preview summary for edits. Error results have this shape: json { \"error\": { \"code\": \"conflict\", \"message\": \"The map revision does not match. Inspect the map before retrying.\" } } Exit code Meaning --- --- 0 Success 1 File access, uncertain live result, or unexpected internal error 2 Invalid command, path, map, batch, or script 3 Existing output or revision conflict 4 Another editor or automation process owns the workspace 5 Input, worker time, memory, or output limit 6 Write attempted in a read-only session 130 Cancelled The file reader is bounded to 64 MiB; the editing API accepts map documents up to 32 MiB and operation envelopes up to 8 MiB. Lua source is limited to 64 KiB. Script-specific limits can report an invalid-script error before the worker limit is reached. Worker execution has a 15-second wall-clock limit, a 256 MiB managed heap cap, and a 512 MiB process-memory threshold sampled during execution. The process threshold is a monitored limit, not an operating-system hard allocation boundary. Only the spawned worker is terminated on timeout or cancellation. Standard output contains the command's JSON result. MCP mode reserves standard output for protocol messages and sends startup errors to standard error. A lost live-job submission response is retried with the same command identity. If the result still cannot be confirmed, uncertain_result asks you to inspect the live document before another edit; a completed job is never blindly submitted under a new identity. Cancellation is also requested when a known live job loses its connection. No command opens a network listener. worker is a private one-request process interface used by the CLI and web server; use apply or run for normal automation. For source validation, build the Release server and CLI, then run: sh node --test metroidvania-studio/cli-tests/.test.mjs node metroidvania-studio/tests/run-automation.mjs node metroidvania-studio/tests/run-automation.mjs --performance The browser runner requires Playwright and a Chromium browser. Set PLAYWRIGHT_MODULE when Playwright is installed outside normal module resolution. Windows defaults to the installed Edge browser; other platforms use Playwright's Chromium. METROIDVANIA_STUDIO_BROWSER_CHANNEL selects an installed channel. METROIDVANIA_STUDIO_DOTNET can select the .NET host. The runner creates its own temporary workspace from the public sample and starts a private loopback server. It never attaches to a user's editing session. Browser execution is headless. Successful runs remove their temporary output; failed runs preserve logs under .local/automation-validation for investigation. The optional performance checks measure brush input and dense tile caching separately from script execution. "},{"title":"CLI quick start","href":"cli--quick-start.html","text":" CLI quick start The command line creates and edits map JSON without a browser, display, game engine, or running web server. It uses the same versioned operations and Lua API as the editor. Using a downloaded program ZIP? Start with [Headless from a release download](release-downloads.md); it includes a CLI launcher and needs no npm installation. Install with Node.js 24+ and the .NET 10 runtime: sh npm install --global metroidvania-studio metroidvania-studio help Alternatively, build once from the repository root with Node.js 24+ and the .NET 10 SDK: sh node tools/scripting/build-runtime.mjs dotnet build metroidvania-studio/cli/MetroidvaniaStudio.Cli.csproj -c Release Run the resulting DLL with the .NET 10 runtime: sh dotnet metroidvania-studio/cli/bin/Release/net10.0/MetroidvaniaStudio.Cli.dll help The examples below use metroidvania-studio as shorthand for that command. An installed npm package provides this command directly. The package does not need an engine installation or an AI account. Create a workspace directory, then a new map: sh mkdir workspace metroidvania-studio init --workspace workspace --map maps/world.json --name \"My World\" Create workspace/build-map.lua: lua local hall = studio.room.add { id = \"hall\", name = \"Hall\", x = 0, y = 0, width = 24, height = 12 } studio.tiles.rectangle { roomId = hall, layer = \"foreground\", x = 0, y = 0, width = 24, height = 2 } studio.room.add { id = \"tower\", name = \"Tower\", x = 24, y = 0, width = 12, height = 24 } print(\"Created a hall and a tower\") Preview, then save: sh metroidvania-studio run --workspace workspace --map maps/world.json --script build-map.lua --seed 7 --dry-run metroidvania-studio run --workspace workspace --map maps/world.json --script build-map.lua --seed 7 metroidvania-studio inspect --workspace workspace --map maps/world.json metroidvania-studio validate --workspace workspace --map maps/world.json metroidvania-studio preview --workspace workspace --map maps/world.json --output previews/world.svg metroidvania-studio export-room --workspace workspace --map maps/world.json --room hall --output exports/hall.json The SVG preview shows room rectangles with region colors and white outlines. It is a headless layout overview; it does not render tile textures or reproduce the interactive minimap's entrance artwork. All input and output filenames are relative to --workspace. Lua source files, batch JSON, map files, room exports, and SVG files remain inside that directory. Paths through symbolic links or junctions are rejected. Existing output files are preserved unless an export explicitly uses --force; map creation never overwrites a file. To work in a live browser session, pass its local URL and use @active as the map: sh metroidvania-studio inspect --url http://127.0.0.1:18765 --map @active metroidvania-studio run --url http://127.0.0.1:18765 --workspace workspace --map @active --script build-map.lua --dry-run Use the port shown by the web launcher. The script file is read from --workspace; the edit targets the document open in that browser session. Live edits share the editor undo history and refuse a conflicting document revision. Close the web editor before editing its files directly. CLI writes use the web server's workspace lock and refuse to overwrite a live session. For connected editing, use the [live API](../api/live-api.md). --dry-run previews do not save files or acquire the writer lock. --read-only refuses writes for the entire command or MCP session. Each command prints one JSON result. Save the revision from inspect and pass --expected-revision when applying a previously prepared edit. A stale revision fails without changing the map. Scripts and batches commit as one atomic file replacement; errors, cancellation, and worker limits leave the original map untouched. See [commands and exit codes](commands.md), [Lua scripting](../scripting/quick-start.md), and [MCP setup](../mcp/setup.md). "},{"title":"Headless from a release download","href":"cli--release-downloads.html","text":" Headless from a release download The desktop EXE opens the editor window. For commands without a window, use the separate CLI launcher beside it. You do not need to run the editor first. Windows Open a terminal in the extracted download folder: powershell .\\metroidvania-studio-cli.cmd help mkdir maps .\\metroidvania-studio-cli.cmd --workspace ./maps init --map world.map.json .\\metroidvania-studio-cli.cmd --workspace ./maps inspect --map world.map.json The Windows download includes the runtime. The web download uses an installed .NET 10 runtime. Neither CLI launcher requires Node.js or a game engine. The workspace folder must exist before running a map command. macOS and Linux sh bash ./metroidvania-studio-cli.sh help mkdir maps bash ./metroidvania-studio-cli.sh --workspace ./maps init --map world.map.json bash ./metroidvania-studio-cli.sh --workspace ./maps inspect --map world.map.json The macOS and Linux downloads include runtimes for ARM64 and x64. The web download uses an installed .NET 10 runtime. Run a Lua script Copy a Lua example from app/docs/examples/ into your maps folder, naming it layout.lua. Preview the changes before saving: powershell .\\metroidvania-studio-cli.cmd --workspace ./maps run --map world.map.json --script layout.lua --dry-run .\\metroidvania-studio-cli.cmd --workspace ./maps run --map world.map.json --script layout.lua .\\metroidvania-studio-cli.cmd --workspace ./maps validate --map world.map.json On macOS or Linux, replace .\\metroidvania-studio-cli.cmd with bash ./metroidvania-studio-cli.sh. Filenames after --map, --script, and --output are relative to --workspace, not to the launcher location. Arguments with spaces must be quoted. Commands print JSON to standard output and return an exit code. They do not open a browser or desktop window. See [CLI commands](commands.md) for every command and [Lua examples](../scripting/quick-start.md) for script syntax. Use an MCP client Configure a client to launch metroidvania-studio-cli.cmd mcp --workspace ... on Windows, or bash metroidvania-studio-cli.sh mcp --workspace ... on macOS/Linux. Use full paths in your own client configuration. If a Windows client cannot start .cmd files directly, set its command to app/runtime/win-x64/dotnet.exe from the extracted Windows download and put app/metroidvania-studio/cli/MetroidvaniaStudio.Cli.dll first in the arguments, followed by mcp, --workspace, and your workspace path. This starts the same server without a shell. See [MCP setup](../mcp/setup.md). For a map already open in the web editor, use [live mode](../api/live-api.md). Offline mode protects the workspace from concurrent writers. Read the API documentation In the editor, open Help → Documentation or Help → API reference. In the extracted download, open app/metroidvania-studio/dist/docs/index.html directly. Search, navigation, and examples work without an internet connection. "},{"title":"Local npm package","href":"distribution--local-package.html","text":" Local npm package Use this workflow to build and test the npm package from source without publishing it. For a registry install, run npm install --global metroidvania-studio. To build the package from the source repository, use Node.js 24+, npm, and the .NET 10 SDK: sh node tools/scripting/build-runtime.mjs npm run pack:local The resulting archive is builds/npm/metroidvania-studio-1.3.2.tgz. The build checks the source version, matching npm/MCP metadata, required notices, and package contents. builds/npm/latest.json records the archive inventory and npm integrity hash. These are local build outputs. Install the local archive in a separate test folder: sh npm install --offline --ignore-scripts --no-audit --no-fund ./metroidvania-studio-1.3.2.tgz node ./node_modules/metroidvania-studio/bin/metroidvania-studio.mjs version mkdir maps node ./node_modules/metroidvania-studio/bin/metroidvania-studio.mjs --workspace ./maps init --map world.map.json A consumer needs Node.js 24+ and the .NET 10 runtime. The SDK, source repository, Git, engine packages, and browser are not required. No installation scripts run, and the package has no npm dependencies. It contains the portable compiled CLI, its managed dependencies, guides, and license notices. The wrapper searches for a .NET 10 runtime using METROIDVANIA_STUDIO_DOTNET, the command path, DOTNET_ROOT, and the standard Windows installation location. An explicit override must point to a valid host; it never falls back silently. Arguments are forwarded directly without a shell, including spaces and Unicode paths. Standard input/output remains available to MCP, and interrupt/termination signals are forwarded to the child process. MCP client configuration After local installation, configure a client to start the wrapper. Replace the relative arguments with paths appropriate to the client's working directory: json { \"mcpServers\": { \"metroidvania-studio\": { \"command\": \"node\", \"args\": [ \"./node_modules/metroidvania-studio/bin/metroidvania-studio.mjs\", \"mcp\", \"--workspace\", \"./maps\" ] } } } Use --read-only when the client should only inspect maps. The MCP server uses stdio and accesses the selected local workspace. The wrapper does not download runtimes, install packages, or contact a registry during execution. MCP Registry publication metadata is maintained in tools/mcp/server.json. Its npm identifier, version, and name must match the staged package. The package build copies it to server.json inside the npm package. The root package.json deliberately remains private: true; only the package built under builds/npm is the distributable package. "},{"title":"Publication checklist","href":"distribution--publication.html","text":" Publication checklist Publish approved releases from main. Local build and validation commands do not publish, create release tags, log into a registry, or change registry ownership. Before publishing: 1. Review the release changes and validation reports, merge the approved work into main, and confirm the final version. All publication and release tagging must run from the approved main commit. 2. Confirm publishing access to the npm package metroidvania-studio and the MCP namespace io.github.LBarimi/metroidvania-studio. 3. Rebuild the local package from the approved commit. Confirm version.json, npm metadata, CLI version, and tools/mcp/server.json agree. 4. Install the exact .tgz in a clean folder. Exercise CLI edits, dry runs, Lua limits, MCP initialization, tool discovery, cancellation, and workspace boundaries. Verify Windows, macOS, and Linux on native hosts before claiming platform coverage. 5. Inspect the package inventory and license notices. Retain the original BSD 3-Clause, Apache 2.0, and MIT notices supplied for managed dependencies. Keep private source, machine paths, maps, media, engine packages, and build symbols out of the package. 6. Publish the reviewed npm artifact only after explicit approval. Root package publication is disabled by private: true. 7. Confirm the public npm artifact and its mcpName, then authenticate the corresponding MCP Registry namespace and submit the matching metadata with mcp-publisher publish tools/mcp/server.json only after explicit approval. 8. Check the published metadata and install instructions before describing registry installation as available in the README. The MCP Registry stores metadata and expects the underlying npm package to exist first. The registry name must match npm's mcpName; see the [official publication guide](https://modelcontextprotocol.io/registry/quickstart). Package files and executable mapping follow npm's [package manifest documentation](https://docs.npmjs.com/cli/v11/configuring-npm/package-json/). Documentation website The [documentation website](https://lbarimi.github.io/metroidvania-studio/) is served by GitHub Pages from the main branch's /docs folder. No separately hosted server or repository workflow file is needed. Edit the Markdown sources in docs/, then run: sh node tools/docs/build-pages.mjs node --test tools/docs/build.test.mjs Review and commit the generated site together with its source changes. Merge into main and push to update the website. Feature branches can prepare and preview the files; Pages publishes only main. Generated downloads live in docs/assets/; edit their original files in the documentation source directories or contracts directory. docs/.site-files records generated files so the build can remove obsolete site outputs without removing Markdown sources. For a standalone offline copy, run node tools/docs/build.mjs and open builds/docs/index.html. Offline builds keep relative links and do not depend on the public website. Building either site locally does not publish it. "},{"title":"Game Preview","href":"game-preview.html","text":" Game Preview Game Preview sits in the upper-right corner of the map canvas. It shows the selected room using the map's PPU and reference resolution while you continue editing. Tile strokes appear before you release the mouse. Other rooms, the editing grid, room borders and selection handles are hidden in Preview. - Use − to collapse the window and + to expand it. - Use the square button to maximize Preview within the map canvas. Press it again or Esc to restore its size. - Select Game camera in the left Tools panel to show a white camera frame and central crosshair in the active room. Drag anywhere inside that frame to move the camera; the pointer becomes a hand and Preview follows immediately. - Turn on Always show game camera beside Frame room to keep the frame visible while painting or using other tools. This viewing preference is remembered locally; the frame does not intercept those tools. - Drag the room's title strip to move it, or its outer handles to resize it, with any tool selected, including Game camera. Locked rooms remain protected. - Drag inside Preview to move the same camera. Press F while it is focused, or use the center button, to return to the selected room's center. - Pixel Perfect snaps camera movement to source-pixel steps. Uncheck it for smooth movement in both Preview and the white camera frame. It starts checked and remembers your choice in this browser. - Change PPU or Resolution in the main toolbar. Preview updates automatically. Source tiles remain 16 pixels wide, so changing PPU changes world units rather than the number of tiles in a fixed pixel resolution. Preview preserves the full camera aspect ratio. It uses integer magnification when space allows and nearest-neighbor reduction when the reference resolution exceeds the window size. Maximize it to inspect more detail. Panning or maximizing Preview leaves your editing camera unchanged. Selecting another room starts Preview at that room's lower-left corner. Collapsing it or switching to the minimap pauses its rendering. The collapsed setting is remembered on this browser. Preview does not run game logic or simulate a player. Held tile strokes update Preview once per animation frame. At native or integer magnification with Pixel Perfect enabled, only changed regions and their neighboring tiles are redrawn. Reduced or smooth views reuse the full-frame rendering path to preserve pixel edges. Camera, room, texture and viewport changes refresh the whole view. The camera stops at the room edges in either mode. If a room is narrower or shorter than the camera, that axis stays at the room's left or bottom edge. The editor clips the white box to the room; Preview keeps the full resolution and shows empty space to the right or above it. Camera positioning is a local viewing aid and does not modify map JSON or Undo history. Choose another tool or press Esc to leave Game camera. It is available on every layer, including locked layers and rooms. Resizing a room or changing resolution clamps the camera to the updated bounds. "},{"title":"Guides and reference","href":"guides.html","text":" Guides and reference Create connected rooms, paint autotiled terrain, and design minimaps with Metroidvania Studio. These guides cover visual editing, portable JSON maps, and automation through Lua, the CLI, MCP, and the editing API. Start editing Guide What you can do --- --- [Sample world](sample-world.md) Try painting, arranging rooms, and navigating the minimap [Room layout](room-layout.md) Add rooms, resize them, and move selected rooms together [Tile palettes and autotiling](tilesets.md) Import PNGs and set up 4-tile or 47-tile palettes [Minimap design](minimap.md) Set room colors, adjust outlines, and return to a room for editing [Texture editing](textures.md) Update shared PNG files and export portable resources [Project bundles](project-bundles.md) Download a map with its palettes and textures in one ZIP [Game Preview](game-preview.md) Check the selected room while editing [Merge and split rooms](room-restructuring.md) Restructure rooms while preserving world contents [Objects and triggers](objects-and-triggers.md) Paint object regions and describe triggers Scripts and automation Lua scripts, the headless CLI, the local MCP server, and the editing API use the same validated operations and map JSON format. Guide Use it for --- --- [Lua quick start](scripting/quick-start.md) Generate or modify rooms with a script [Headless from a download](cli/release-downloads.md) Run commands from the extracted program download [CLI quick start](cli/quick-start.md) Edit, validate, export, and preview maps without a browser [MCP setup](mcp/setup.md) Give an agent local map editing tools [Building from source](building.md) Build, run, and refresh local web bundles [Editing API](api/index.md) Apply structured, atomic operation batches [Live web API](api/live-api.md) Work on the document currently open in the studio [Local npm package](distribution/local-package.md) Build and test an installable package from source Open and save maps Use File → Open map to choose a JSON map from your computer. Save map or Ctrl+S writes back to that file; Save map as lets you choose another name or folder. Import map JSON opens a copy with no link to the original file. Room JSON exports and workspace recovery continue in the background. Chrome, Edge, and the Windows program support the file picker workflow. In browsers without a writable file picker, saving starts a JSON download using the browser's download settings. A download does not mark the document saved because the editor cannot confirm its completion. In the web editor Open File → Scripts. Write a script or load a UTF-8 .lua file, then select Run. The seed makes randomized scripts repeatable. Dry run validates the complete result without changing the map. Cancel discards an unfinished job. A successful run becomes one Undo step and uses the existing autosave, room export, and engine synchronization flow. You can keep editing while a script runs. If the document changes first, the script result is rejected instead of overwriting your edits. Scripts use [documented studio functions](scripting/api-reference.md), not file access or engine APIs. Read [execution limits](scripting/execution-limits.md) before generating large maps. Choose a document owner Use file mode for a workspace that the web editor is not using. For a document open in the web editor, connect the CLI or MCP server with --url and use --map @active. This preserves the web editor's history and synchronization. Separate workspace copies can be edited independently. The CLI emits JSON on stdout. MCP uses stdio; it does not expose a network port. Live mode connects only to an explicitly selected loopback URL. Data compatibility Automation API version 1, studio version 1.3.2, and map format version 2 are separate version numbers. Existing map files and engine packages continue using [map format 2](../metroidvania-studio/contracts/FORMAT.md). Lua scripts are editing tools and are not embedded in exported game data. See [validation commands and platform coverage](validation.md) to run the checks locally. Install the CLI and MCP tools with npm install --global metroidvania-studio. Running the package requires Node.js 24+ and the .NET 10 runtime. Building or installing a local package does not publish it. Browse the reference Read the [online documentation](https://lbarimi.github.io/metroidvania-studio/), or open Help → Documentation in the studio for searchable offline guides. Help → API reference opens the editing API. The bundled pages are also available at app/metroidvania-studio/dist/docs/index.html inside a release download. From source, run node tools/docs/build.mjs and open builds/docs/index.html. "},{"title":"Metroidvania Studio","href":"index.html","text":" Metroidvania Studio Metroidvania Studio is a free, open-source 2D level editor for metroidvania games. Build connected rooms, paint autotiled maps, and design minimaps with JSON export for multiple game engines. Runs locally on Windows, macOS, and Linux. No game engine installation is required to edit your maps. [Download the studio](https://github.com/LBarimi/metroidvania-studio/releases/latest) · [Start with the sample world](sample-world.md) · [Browse the documentation](guides.md) Automate map creation with [Lua scripts](scripting/quick-start.md), a [headless CLI](cli/quick-start.md), and a [local MCP server for AI agents](mcp/setup.md). Create connected rooms Use the room-based map editor to paint terrain, connect passages, and adjust your layout. Move, resize, or rotate rooms as your world takes shape. ![Add a room, paint rock terrain, and connect it to a neighboring room](assets/create-and-paint.gif) [Room layout guide](room-layout.md) · [4-tile and 47-tile autotiling](tilesets.md) Design your minimap Give each area a color, adjust room outlines and entrances, and see how the world connects. Double-click a room in the minimap to return to editing it. ![Explore a connected minimap, open a room, and zoom out in the map editor](assets/design-the-minimap.gif) [Minimap guide](minimap.md) · [Check your framing with Game Preview](game-preview.md) Get started 1. Download the package for your platform from GitHub Releases, then extract it. 2. Open the launch file at the top level. The prebuilt web package requires ASP.NET Core Runtime 10. 3. Try the sample world. Select a room and left-drag to paint; middle-drag to pan and use the wheel to zoom. 4. Choose File → Save map as to save your work as JSON. Prefer building from source? See the [build requirements and platform scripts](building.md). Use your maps in your engine Export a shared JSON format and import it with an engine package. Maps and textures stay in your files; gameplay belongs in your game project. Engine Package and installation --- --- Unity [Unity package](https://github.com/LBarimi/metroidvania-studio/tree/main/engine-packages/unity) Godot [Godot addon](https://github.com/LBarimi/metroidvania-studio/tree/main/engine-packages/godot) Unreal Engine 4 / 5 [UE4 plugin](https://github.com/LBarimi/metroidvania-studio/tree/main/engine-packages/ue4) · [UE5 plugin](https://github.com/LBarimi/metroidvania-studio/tree/main/engine-packages/ue5) SDL3 [C++ integration and sample](https://github.com/LBarimi/metroidvania-studio/tree/main/engine-packages/sdl) [Map JSON format](../metroidvania-studio/contracts/FORMAT.md) · [Texture files and engine resources](textures.md) Scripts, CLI, and AI agents Run Lua in File → Scripts, work without opening the editor through the headless CLI, or let an AI agent edit maps through the local MCP server. These interfaces share validated editing operations, with dry runs and revision checks to review changes. [Lua quick start](scripting/quick-start.md) · [Headless from a download](cli/release-downloads.md) · [MCP setup](mcp/setup.md) · [Editing API](api/index.md) Questions before you start Do I need a game engine to use the editor? No. The studio runs independently. Install an engine package when you want to import a map into your game project. Where are my maps and textures saved? In Maps and Textures beside the launcher or at the source project root. Use File → Storage folders to find them. Keep Maps, Textures, and .studio when updating the studio. Is it free to use? Yes. Project-owned code and assets use the [MIT License](https://github.com/LBarimi/metroidvania-studio/blob/main/LICENSE). External components retain their [respective license notices](https://github.com/LBarimi/metroidvania-studio/blob/main/THIRD-PARTY-NOTICES.md). "},{"title":"MCP setup","href":"mcp--setup.html","text":" MCP setup Metroidvania Studio exposes a local stdio MCP server for map inspection, batch edits, Lua scripts, JSON exports, and headless layout previews. The client starts a child process and communicates through its standard input and output. No port, HTTP endpoint, remote service, API key, or AI model is required by this server. Install the CLI with Node.js 24+ and the .NET 10 runtime: sh npm install --global metroidvania-studio Alternatively, build from source using the [CLI guide](../cli/quick-start.md). Configure a client to run: sh metroidvania-studio mcp --workspace workspace For clients that launch npm packages through npx, pin the version in the configuration: json { \"mcpServers\": { \"metroidvania-studio\": { \"command\": \"npx\", \"args\": [\"-y\", \"metroidvania-studio@1.3.2\", \"mcp\", \"--workspace\", \"workspace\"] } } } npx may download the package on first use. Clients that cannot launch Windows command shims can use node with the installed wrapper path, as shown in the [local package guide](../distribution/local-package.md). Set the client's working directory so workspace resolves to the intended folder, or configure the workspace path explicitly in your own client settings. The directory must already exist. When running a source build directly, set command to dotnet and put the built MetroidvaniaStudio.Cli.dll path first in args. Add --read-only to permit inspection, validation, previews, and dry runs while rejecting file writes. Each process has one fixed workspace root. Client root notifications cannot grant access outside it, and the server does not discover or read unrelated directories. Map paths and export paths must be relative to that workspace; traversal, symbolic links, junctions, and internal session files are rejected. The server uses the official MCP C SDK for protocol negotiation, JSON-RPC, tool discovery, notifications, and request cancellation. It supports the SDK's current discovery flow and compatible initialize clients. Each incoming JSON line is limited to 4 MiB. Send large batch workflows through files and the CLI instead of one oversized MCP message. The MCP server performs no external network requests. Optional connected mode sends requests only to the explicitly selected loopback web editor. Your chosen AI client may send tool inputs and results to its model provider according to that client's settings. Use a local model/client configuration when all map content must remain on your machine. Suggested workflow: 1. Call studio_capabilities for operation schemas. 2. Call studio_inspect to obtain room IDs and the current file revision, or studio_create for a new map. 3. Call studio_apply or studio_run_lua with that expectedRevision and dryRun: true. 4. Review the proposed layout and call again with dryRun: false to save. 5. Call studio_preview and studio_validate, then export the required room JSON. To edit a document already open in the browser, configure the server with mcp --url http://127.0.0.1:18765 instead of --workspace. Use the port printed by the web launcher. Pass map: \"@active\" to inspection, batch, Lua, validation, and preview tools. Revision checks prevent changes from overwriting intervening browser edits, and successful edits appear in the web undo history. The MCP client forwards cancellation to the live job. Connected mode supports these active-document tools; new map files and room exports use offline mode. Close a web editor session before directly editing the same workspace through an offline MCP server. The shared writer lock rejects concurrent writers. Use the live web API for editing an open browser session. See [tool reference](tools.md) and [Lua guide](../scripting/quick-start.md). Registry and npm publishing are separate release steps; a local build works before either is published. "},{"title":"MCP tools","href":"mcp--tools.html","text":" MCP tools Tool Main arguments Behavior --- --- --- studio_capabilities None API version, operation schemas, and limits studio_create map, optional name, dryRun Create an empty map; never replace an existing file studio_inspect map Room/object IDs, geometry, properties, camera, and file revision studio_query_tiles map, roomId, layer, optional x, y, width, height Read up to 4096 tile positions, including shapes and resource IDs studio_validate map Validate the map data contract studio_apply map, batch, expectedRevision, optional dryRun Apply an atomic versioned operation batch studio_run_lua map, source, expectedRevision, optional seed, dryRun Execute bounded Lua in a worker and commit one edit studio_export_room map, roomId, output, optional force, dryRun Export one room as an independent JSON document studio_preview map Return a headless SVG room-layout overview without writing files Tool discovery includes descriptions, JSON input schemas, and read-only/destructive/idempotent annotations. All tools declare a closed workspace domain. Annotations help clients present actions; the actual path checks and read-only policy are enforced independently. Successful calls return the same data in structuredContent and a JSON text content block. Domain errors set isError: true and return error.code and error.message. Unknown methods and invalid protocol requests use JSON-RPC errors. For bounded tile reads, see [tile queries](../api/queries.md). See the [CLI error codes](../cli/commands.md) for domain error meanings. With --url, use map: \"@active\" for the currently open web document. Live inspection returns a content hash as revision; the client also carries the web instance and numeric document revision in each job. studio_create and studio_export_room are offline file tools and are unavailable in connected mode. For an edit, expectedRevision must be the 64-character SHA-256 value returned by studio_inspect. A stale revision returns conflict and writes nothing. Read the latest state before deciding whether to retry; do not automatically replace the revision on an old destructive operation. Batch example: json { \"name\": \"studio_apply\", \"arguments\": { \"map\": \"maps/world.json\", \"expectedRevision\": \"REPLACE_WITH_REVISION_FROM_INSPECT\", \"dryRun\": true, \"batch\": { \"apiVersion\": 1, \"operations\": [ { \"op\": \"room.add\", \"id\": \"hall\", \"name\": \"Hall\", \"x\": 0, \"y\": 0, \"width\": 24, \"height\": 12 } ] } } } Use the MCP client's cancellation action to stop an active script or batch. The worker is stopped and its uncommitted changes are discarded. A cancellation notification may suppress the original response; the connection remains usable for the next request. Cancellation received after an edit has already committed cannot undo that completed edit. studio_preview returns mimeType: \"image/svg+xml\" and an SVG string. It represents the room layout and region colors without loading textures, running game logic, opening a browser, or capturing a desktop window. It does not reproduce the interactive minimap entrance styling. "},{"title":"Minimap design","href":"minimap.html","text":" Minimap design Review your world as colored rooms with white outlines and entrance marks. The minimap follows the room layout and lets you jump directly to a room for editing. Navigate and edit Switch to the minimap using the view tabs. Click a room to select it, or double-click it to return to the map editor with that room focused. Middle-drag to pan and use the mouse wheel to zoom. The toolbar provides whole-world framing and selected-room framing. Enable room names when you need labels while inspecting a large world. Room colors Select a room and change its region color in the inspector using the color swatch or HEX field. The minimap uses that color for the room's interior. Unselected room interiors appear dimmer. White outlines and entrance marks stay clear for both selected and unselected rooms. Outlines and entrances The minimap toolbar has separate Outline and Entrance length controls. Their defaults are 9 and 15. Both scale with zoom so their proportions remain consistent as you zoom in or out. Entrance marks follow the room connections calculated from the layout. To change which rooms meet, return to the map editor and adjust their positions or boundaries. See [Room layout](room-layout.md). Try a complete world Open Help → Open sample world and follow its connected rooms in the minimap. Double-click one room, edit its layout, then return to the minimap to inspect the result. The [sample walkthrough](sample-world.md) covers the full sequence. "},{"title":"Objects and trigger events","href":"objects-and-triggers.html","text":" Objects and trigger events The Objects palette contains Spawn, Portal, Invisible wall, Path, and Respawn point. Portals are magenta room-transition regions: choose Portal in the Objects palette, then click for one tile or drag to paint a path. Use the Triggers layer to draw a rectangular event region. Previously placed legacy objects stay in existing maps. Every placed object, trigger, and decal has a unique ID. IDs are hidden in the inspector and retained in JSON exports. Newly generated IDs contain only decimal digits (up to 16 digits), stored as strings for compatibility. Existing IDs and explicitly supplied script IDs remain unchanged. Copying an object or a room assigns new IDs to the copies. Moving, resizing, saving and reopening keep the original IDs. Room title handles and resize handles work on every layer. Locked rooms remain protected. Portals and invisible walls Spawn, Respawn point, Portal and Invisible wall all paint one object per 16-pixel tile cell along the cursor path. Occupied cells on the same object layer are skipped, including hidden or locked groups. Each cell keeps its own unique ID; one Undo reverses the entire stroke. Painting stays inside the starting room. Existing larger regions remain intact. Invisible walls are translucent blue in the editor and hidden in Game Preview. Engine imports preserve each region's ID, definition, bounds and description. Portal detection is independent of numbered trigger events; no room transition or collision callback is installed automatically. Old portal event properties remain in imported data for compatibility but are ignored. Engine Portal Invisible wall --- --- --- Unity Trigger collider with StudioPlacedObject.IsPortal Solid collider with IsInvisibleWall; no renderer Godot Area2D with authored data in get_meta(\"object\") StaticBody2D with a collision polygon; no visual Unreal Engine Overlap-only collision tagged with the object ID and definition Blocking collision without a mesh surface SDL Room.primitives entry with trigger=true and definition=\"Portal\" solid=true, invisible=true; excluded from drawing In SDL, use these polygons in your game's collision handling. In every engine, the game owns player filtering and portal behavior. Edit a trigger 1. Select the Triggers layer and drag an area. 2. Select its Event, from Trigger001 through Trigger200. 3. Once is checked for new triggers. Uncheck it to allow repeated event requests within a manager session. Existing trigger settings are preserved. 4. Enter a Description and click Apply properties. Descriptions appear at the center of objects and trigger regions in the map editor. They are hidden in Game Preview. Portals have a description, but no Event or Once setting. They never dispatch through the trigger manager, including when imported JSON contains old event properties. Your game decides what happens on portal contact. None means unassigned. Old free-text event values remain in imported maps and are shown as existing events. Select a numbered event before requesting them through a runtime manager. Runtime contract Engine packages include a trigger manager. Your game decides when to request an event, for example after its own collision check or interaction button. Importing a room does not connect collision callbacks to event delivery. Each successful request supplies: Value Meaning --- --- Room ID and name The authored room containing the object Room X and Y The room origin in tile coordinates, X right and Y up Object ID The unique ID of the trigger Definition For example, Area Event Stable enum: None = 0, Trigger001 = 1, through Trigger200 = 200 Once Whether repeated requests are blocked Description The authored description Room coordinates do not change with PPU or engine coordinate conversion. Object-local position and bounds remain available in the imported object data. The map format remains version 2. Trigger settings use the existing properties array: json \"properties\": [ {\"key\":\"event\",\"value\":\"Trigger037\"}, {\"key\":\"once\",\"value\":\"true\"}, {\"key\":\"desc\",\"value\":\"North gate\"} ] Event numbers have the same meanings in all packages. The game assigns behavior to them; the studio does not implement transitions, spawning, quests, or save-game rules. Unity An imported room exposes StudioImportedRoom.Triggers. Subscribe once and request an object's ID when your game decides it has activated: csharp var manager = importedRoom.Triggers; manager.TriggerRequested += info => HandleEvent(info); bool delivered = manager.TryRequest(objectId, out var info); StudioPlacedObject.TryRequestTrigger(out info) is a shortcut for its containing room. StudioTriggerEvent and StudioTriggerInfo are in MetroidvaniaStudio.Integration. For multiple loaded rooms, create one StudioTriggerManager for the world and pass it as the optional fourth argument of StudioRoomBuilder.Build. This lets every room use the same subscription and one-shot state. Unreal Engine An imported room actor exposes a Trigger Manager component. In Blueprint, bind On Trigger Requested, then call Request Trigger with the authored Object ID. The result is true only when the event was delivered. Get Trigger reads its information without consuming it. The payload uses FStudioTriggerInfo; RoomPosition is an integer X/Y pair and Event is EStudioTriggerEvent. In C++, call Room->TriggerManager->RequestTrigger(ObjectId) after including MetroidvaniaStudioTriggerManager.h. To centralize a world, add a Metroidvania Studio Trigger Manager component to a persistent actor and call Register Room with each loaded document JSON and room ID. Subscribe to that component and direct your requests to it. Both Unreal packages use this API. Godot An imported room contains a TriggerManager node. Connect its signal once: gdscript var manager = imported_room.get_node(\"TriggerManager\") manager.trigger_requested.connect(handle_event) manager.request_trigger(object_id) func handle_event(info: Dictionary) -> void: print(info.roomId, info.objectId, info.event, info.description) The dictionary contains roomId, roomName, roomX, roomY, objectId, definition, event, once and description. Enum constants are available through MetroidvaniaStudioTriggerEvent.Id. For a persistent world manager, create a MetroidvaniaStudioTriggerManager, call register_room(room_dictionary) for each room, and connect its trigger_requested signal. Authored registrations survive saving an imported scene. SDL SdlRoom.triggers registers the loaded room. Use the manager after a successful Load: cpp room.triggers.onTriggered = [](const MetroidvaniaStudio::TriggerInfo &info) { HandleEvent(info); }; MetroidvaniaStudio::TriggerInfo info; bool delivered = room.triggers.TryRequest(objectId, info); For several rooms, use one MetroidvaniaStudio::TriggerManager and call RegisterRoom(room.data) on each loaded room. Its callback receives the same information as the other engines. The manager is independent of rendering and does not poll SDL input or collision state. One-shot lifetime A manager consumes a Once event before calling the subscriber, so a recursive request cannot deliver it twice. Queries do not consume anything. Unassigned, missing, or already consumed IDs return false without delivery. Portals and invisible walls are excluded from this manager. Re-registering or unregistering a room on the same manager preserves consumed IDs. Use ResetOnce / reset_once to reactivate one ID, or ResetAllOnce / reset_all_once for all IDs. Clear / clear removes registrations and consumed state. A new manager starts a new session; a game that needs persistence across scene changes should keep its world manager alive and save its own progression. Duplicate object IDs across registered rooms are rejected before replacing that room's registrations. Request and subscription methods are intended for the game thread. Subscribe and unsubscribe according to your game's object lifetime. "},{"title":"Export a project bundle","href":"project-bundles.html","text":" Export a project bundle Use File → Export project bundle… → Download ZIP to share the current map with its palettes and textures. The ZIP contains all rooms, including edits you have not saved to a named map yet. The dialog shows progress and lets you cancel. Downloading a bundle does not change the open map, its save status or Undo history. The browser or Windows application's download settings choose where the ZIP is saved. What the ZIP contains text Maps/ world.map.json .studio/ catalog.json Textures/ ...referenced images... readme.txt - world.map.json contains the complete current map: room IDs, positions, both tile layers, objects, descriptions, minimap properties and camera settings. - catalog.json contains the workspace's palette groups, palettes, autotile rules and object definitions. - Textures contains images referenced by those definitions or the map's backdrops. Both generated atlases and original 4-tile / 47-tile source PNGs are included. Images supplied by the application are copied into the bundle too. All registered palettes are retained so you can continue painting after moving the project. Other saved maps, unrelated files, recovery data and scripts are not bundled. Resource paths are relative to the extracted folder; the catalog's machine-specific project path is replaced with .. Custom map properties remain unchanged. Continue editing on another computer Extract into a new folder and start the studio with that folder as its workspace. For example, from a Windows source checkout, if the extracted folder is named world-bundle: bat platform\\win\\web\\run.bat --project \".\\world-bundle\" The --project option also works with the other platform launchers and the Windows program. Use the extracted folder's location as its value. The studio opens the exported map and resolves palettes from the bundled catalog. Opening only world.map.json in an already running, different workspace keeps that workspace's palettes. To use everything in the bundle together, select the extracted folder with --project. ZIP import is not a separate menu action in this version. Use it with an engine Extract the ZIP before using your engine package's JSON import. Select: Import setting Choose --- --- Map JSON Maps/world.map.json Catalog .studio/catalog.json Resource root The extracted folder containing Maps, .studio and Textures The map and catalog retain their existing JSON formats. No engine-package update is required for this bundle layout. If export cannot finish A missing or unreadable image stops the export and shows its resource name. Finish saving that image or fix its palette reference, then retry. If an image or palette changes during export, retry after the change finishes. Bundles support up to 256 MiB before compression, 32 MiB per image and 4096 images. Names that would collide or be invalid on another supported operating system must be corrected before exporting. "},{"title":"Room layout","href":"room-layout.html","text":" Room layout Build a connected world by adding rooms, resizing their bounds, and arranging them on the map canvas. Move rooms individually or select several rooms to reposition a whole section. Add and select rooms Right-click empty space in the map editor and choose the room creation action. The new room is placed near that position without overlapping existing rooms. Its suggested size covers the current reference resolution, rounded up to whole tiles: 320 × 180 starts at 20 × 12 tiles, and 640 × 360 at 40 × 23. You can change the dimensions before creating it. A new blank map also keeps the current camera settings and starts with a room sized to match. Click a room to select it. While painting, the first left or right click in a different room only activates it. Start another stroke to paint or erase there. The selected room's properties appear in the inspector. Move and resize Drag a room's title strip to move it. Bring room edges together to snap them into place. Drag the outer resize handles to change the room's bounds. These handles work with every tool and layer, including Game camera. The inspector also provides dimensions, rotation and reflection controls. When shrinking a room, existing contents limit how far its bounds can move inward. Rooms touching a resized edge may move with it to preserve their layout. Rooms separated by a gap stay in place; a resize stops if it would overlap them. Hold Ctrl and click rooms to build a selection, then drag a selected room's title strip to move the selection together. Click empty space to clear the multiple selection. Use Edit → Undo to undo a layout change. Navigate the world Middle-drag to pan and use the mouse wheel to zoom. Right-drag on empty space also pans; a right click without dragging opens the room creation menu with any tool selected. Use Fit map or Frame room in the toolbar to focus the editing canvas. The PPU and Resolution toolbar controls set the camera framing in [Game Preview](game-preview.md), the window in the canvas's upper-right corner. Select Game camera in Tools to drag its white frame, or turn on Always show game camera to see it while painting. The camera starts at the room's lower-left corner; the Center control moves it to the room's center. Imported engine cameras use the same starting corner. Paint connected spaces Select a tile palette and paint the room's foreground or background layer. Autotiling updates neighboring tile edges as you draw. Use [tile palette settings](tilesets.md) to choose your source images and connection mode. Open the [minimap](minimap.md) to review the overall layout. Room adjacency describes the map layout; gameplay transitions are implemented in your game project. Fit to resolution In the room properties, choose Fit to resolution to size the room for the current reference resolution. Room dimensions round up to whole 16-pixel tiles: 320 × 180 becomes 20 × 12 tiles. The room keeps its position. When contents would be removed, a warning shows the number of affected tiles and objects before applying the change. Both tile layers and all object layers are included. An object is deleted entirely if its transformed body or any path node extends outside the new bounds. Cancel keeps everything; Undo restores the room, deleted contents and any attached rooms moved by the resize. "},{"title":"Merge and split rooms","href":"room-restructuring.html","text":" Merge and split rooms Merge selected rooms Hold Ctrl or Cmd and click the rooms to select them together. Choose Edit → Merge selected rooms, or use the same action in the inspector. The result is one rectangular room enclosing the selection, including any space between the selected rooms. The active room keeps its ID, name and region color. Foreground tiles, background tiles, objects, triggers and path nodes keep their world positions. Placement IDs and per-object properties stay unchanged. The merge is rejected if its rectangle would overlap an unselected room, if selected rooms already overlap, or if a selected room is locked. Different values for the same custom room property must be resolved first. Room-specific backdrop filters must also agree. Split a selection into a room Choose the selection tool and drag a rectangle inside the active room. Choose Edit → Split selection into a room, or use the same action in the inspector. The selected rectangle becomes a new room. The remainder is partitioned into up to four rectangular rooms without overlap or gaps. The largest remaining part keeps the original room ID and name; the other rooms get unique IDs and names. The new selected room is focused automatically. Splitting includes every tile and object layer, including hidden layers and groups. All content stays at its original world position. Complete objects, trigger rectangles, decals and path nodes move together into the room that contains them. If a cut would cross one of these elements, adjust the selection or move the element first. Room properties are copied to each part. Compatible backdrop filters are extended to the new rooms. Custom property values are opaque: references inside your own property strings are not rewritten automatically. Save and undo Each merge or split creates one Undo step. Room JSON exports and the minimap update through the normal editing flow. Undo restores the original rooms and placement IDs; Redo restores the same generated room IDs. The exported data continues to use map format 2. "},{"title":"Try the sample world","href":"sample-world.html","text":" Try the sample world A fresh workspace opens Starter World. To reopen it later, choose Help → Open sample world. If your current map has unsaved changes, save it first or use the confirmation dialog to discard those changes. Opening the sample creates an editable copy; the bundled original stays intact. A five-minute tour 1. Paint Landing. Select the first room, then drag the brush along the floor. Try a diagonal shape or right-drag to erase. Use Undo to restore your edit. 2. Explore Canopy. Select the second room. Toggle the background layer's eye icon to compare its terrain with the backdrop. 3. Arrange the upper route. Ctrl-click Sky Bridge and Lift Shaft. Drag a selected room's title strip to move the pair together. Undo, then try resizing a room using its outer handles or rotating it in the inspector. 4. Visit Foundry. Select the objects or triggers layer, then move the marker or resize the trigger rectangle. These are sample data for editing; they do not implement game behavior. 5. Read the minimap. Follow the loop from Landing through Canopy, Lift Shaft, Sky Bridge and Lookout. Double-click a room to return to the map editor at that room. Keep experimenting Use the palette's + button to add a color for your own area. File → Save map as saves your edited world to a JSON file. Room exports continue automatically in the workspace. To try external image editing, follow [Texture editing and engine resources](textures.md). The sample uses the standard catalog and image files. Your custom catalogs and existing saved maps take priority over the sample at startup. The world data is also available as samples/maps/starter-world.map.json in the source download. No game engine or network connection is needed to explore it. "},{"title":"Lua API reference","href":"scripting--api-reference.html","text":" Lua API reference Scripts use a restricted Lua 5.2 interpreter and studio.api_version == 1. The automation API version is separate from the application's version and the map JSON format version. Inspect the input map - studio.document() returns document metadata, camera settings, room summaries, properties, and minimap connections. - studio.rooms() returns the room summaries as a Lua sequence starting at index 1. - studio.log(...) and print(...) collect messages shown in the result. - studio.tiles.get { roomId, layer, x, y, width, height } reads a rectangular tile window from the input snapshot. The window must lie inside the room and cover at most 4,096 cells. The returned cells sequence contains only occupied cells with x, y, shape, materialId, and groupId. Missing coordinates are empty. Room summaries include roomId, name, x, y, width, height, visible, locked, foregroundCount, backgroundCount, objectCount, properties, and objects. Object summaries include objectId, definitionId, layer, x, y, width, and height. These are detached snapshots of the map before the script started. Changing a returned table does not edit the map. Queued operations do not appear in later snapshot queries. Keep returned IDs and calculated positions in local variables while constructing a batch. Edit the map Each method accepts exactly one table. The table fields match the common automation API; the method name supplies op automatically. Lua method Required fields Common optional fields --- --- --- studio.room.add x, y, width, height id, name studio.room.update roomId name, visible studio.room.move roomId, x, y studio.room.resize roomId, width, height x, y, crop, keepLocalContents studio.room.rotate roomId clockwise studio.room.duplicate roomId, x, y id, name studio.room.delete roomId studio.tiles.paint roomId, layer, cells materialId, groupId studio.tiles.erase roomId, layer, cells groupId studio.tiles.rectangle roomId, layer, x, y, width, height materialId, shape, erase, groupId studio.tiles.fill roomId, layer, x, y materialId, shape, erase, groupId studio.properties.set values roomId studio.camera.set ppu, width, height studio.object.add roomId, definitionId, layer, x, y id, width, height, groupId, properties studio.object.update roomId, objectId x, y, width, height, rotation, scaleX, scaleY, definitionId, properties studio.object.delete roomId, objectId studio.rename name studio.room.add, studio.room.duplicate, and studio.object.add return the created ID. Pass explicit IDs when another script or external system needs stable names. Otherwise the runner assigns IDs without colliding with existing rooms or objects. studio.apply { op = \"room.add\", ... } accepts the common operation form directly. It has the same limits and validation as named methods. Use studio.rename { name = \"World\" } for the document.update operation. Tile layers are \"foreground\" and \"background\". Object layers are \"entities\", \"triggers\", \"foregroundDecals\", and \"backgroundDecals\". A cell is { x = 0, y = 0, shape = \"solid\" }; shape can also be \"bottomLeft\", \"bottomRight\", \"topLeft\", or \"topRight\". Paint defaults to material \"terrain\" and shape \"solid\". Array fields use consecutive integer keys starting at 1. Room/world coordinates and tile cells are integers. Objects support fractional local coordinates. Each resource uses a catalog ID such as materialId or definitionId, not a filesystem path. Use strings for property values. To remove a property, pass studio.null; assigning Lua nil removes the table entry before the operation reaches the API. lua studio.properties.set { roomId = \"entry\", values = { region = \"Garden\", temporary = studio.null } } Resize preserves content by default. crop = true explicitly permits cropping. Overlapping rooms, invalid IDs, out-of-bounds painting, and edits to protected content fail the whole batch. Available Lua functions Lua control flow, local functions, tables, arithmetic, concatenation, and comparisons are available. Basic functions include assert, error, ipairs, pairs, next, select, tonumber, tostring, and type. - math supports numeric helpers and seeded random. randomseed is unavailable; choose the seed when running the script. - bit32 supplies bounded numeric bit operations. - string.len, sub, rep, lower, upper, reverse, and find are available. find searches literal text; patterns are unavailable. Use function syntax such as string.sub(value, 1, 3). - table.insert, remove, and concat operate on sequences. String helpers use the interpreter's UTF-16 text representation. Use ASCII when indexing by individual character positions. Unicode names and property values are preserved when passed as whole strings. File I/O, networking, processes, modules, dynamic code loading, host object access, metatables, debug functions, coroutine APIs, and custom error-catching functions are unavailable. See [execution limits](execution-limits.md) before processing large maps. "},{"title":"Script execution limits","href":"scripting--execution-limits.html","text":" Script execution limits Scripts construct one batch of edits against a snapshot. The map is updated only after the script completes and every operation passes validation. A failed or cancelled script returns no partial document. Resource Limit --- --- UTF-8 script source 64 KiB Interpreter instructions 1,000,000 Interpreter execution time 5 seconds Allocations during interpretation 64 MiB cumulative on the interpreter thread Operations 1,024 Tile query window 4,096 cells Serialized operation data 8 MiB, conservatively checked before allocation Converted operation data 200,000 values, at most 32 nested levels Log output 128 entries, 4,096 characters per entry, 16,384 characters total Standard helper strings 65,536 UTF-16 characters Standard helper sequences 100,000 elements Input map 32 MiB UTF-8 JSON The shared API additionally limits the complete request, visited cells, and total work. Prefer rectangle painting over one operation for each tile. For larger construction jobs, split the work into deliberate batches and inspect each result. The runner checks instruction, time, cancellation, and allocation limits between every interpreter instruction. Standard string and table helpers check sizes before allocating. Unbounded native helpers, script callbacks inside native sorting, module loading, and host objects are not exposed. The web and command-line hosts run Lua in a separate worker process with an overall deadline and memory limits. This also covers script parsing and unexpected interpreter failures. The interpreter library alone is not an operating-system sandbox: hosts embedding LuaScriptRunner.Run directly must provide equivalent process isolation for untrusted scripts. Memory limits are resource controls, not a guarantee about all operating-system behavior. Scripts have no API for opening files, making network requests, starting processes, or reading environment variables. The host, not the script, selects the input and output map files. Snapshot queries describe the input map. They do not include operations queued earlier in the same run. Editing a snapshot table has no effect on the result. Use explicit API methods to change the map. The random seed controls math.random, and generated IDs are deterministic for the same input and operations. Time measurements, operating-system random sources, external modules, and math.randomseed are unavailable. Embedding and building The C entry point is MetroidvaniaStudio.Scripting.LuaScriptRunner.Run(documentJson, source, seed, cancellationToken). It returns DocumentJson, Logs, OperationCount, and Changed. It performs no filesystem or network I/O. Always run untrusted scripts in a worker process as the shipped hosts do. Normal build scripts prepare the interpreter automatically. When building the C projects directly, first run: sh node tools/scripting/build-runtime.mjs The bootstrap downloads the exact source revision and verifies its SHA-256 against tools/scripting/source-lock.json. It compiles the interpreter without debug paths and checks the result before use. Source, build output, and the local package stay under ignored .local/scripting-runtime. Subsequent builds reuse a checksum-verified cache; a valid cache needs no download. Generated output records the SDK version and binary hashes without publishing machine paths. The original interpreter assembly name and version are preserved. Its license and provenance files are copied with the CLI and web server. No separate Lua installation is required. "},{"title":"Lua quick start","href":"scripting--quick-start.html","text":" Lua quick start Lua scripts can create rooms, paint terrain, place objects, and adjust map properties in one operation. The editor, command line, and MCP tools use the same editing API. Open Edit > Scripts, paste a script, and choose Preview. Review the result, then run the script to apply it. A successful script creates one undo step. Cancellation, script errors, invalid edits, or a map changed during execution leave the original map unchanged. lua local rooms = studio.rooms() if rooms == 0 then print(\"Create a room first.\") else local room = rooms[1] studio.tiles.rectangle { roomId = room.roomId, layer = \"foreground\", x = 0, y = 0, width = room.width, height = 1 } print(\"Painted a floor in \" .. room.name) end Coordinates are measured in tiles. Room positions are world coordinates; tile and object positions are local to their room. Positive Y points up. The tile sprite size is 16 pixels; camera PPU and resolution are separate settings. Studio functions accept a table with named fields. A room creation returns its ID, which can be used in later operations in the same script: lua local room = studio.room.add { name = \"Hall\", x = 0, y = 0, width = 24, height = 12 } studio.tiles.rectangle { roomId = room, layer = \"foreground\", x = 0, y = 0, width = 24, height = 2 } Start this example in an empty map, or choose coordinates clear of existing rooms. The API rejects overlapping rooms. Use the same seed to repeat a random layout. math.random() and math.random(minimum, maximum) use the run's seed. Generated IDs are also repeatable for the same input map and script. Existing maps are not cleared automatically. Examples: - [Connected rooms](../examples/connected-rooms.lua) - [Seeded platforms](../examples/seeded-platforms.lua) - [Region properties](../examples/region-properties.lua) For command-line execution, see the [CLI guide](../cli/quick-start.md). For exact operations, read the [Lua API reference](api-reference.md) and [execution limits](execution-limits.md). Saved scripts and examples Open File → Scripts. The library lists .lua files from the workspace Scripts/ folder and the bundled examples. Opening an entry loads its code without running it. Select Run to apply it, or enable Dry run to inspect the result first. Use New script, enter a filename, and choose Save to library to keep code in the workspace. Open script imports a file from your computer; Save script downloads a copy. An example is read-only until saved as a workspace script. Refresh list picks up scripts added or changed outside the studio. If another program changes a script before you save, reopen it to review the changes instead of overwriting them. Scripts remain separate from map JSON and engine packages. The library accepts up to 256 files of at most 64 KiB each. Existing maps and autosave behavior are unchanged. "},{"title":"Texture editing and engine resources","href":"textures.html","text":" Texture editing and engine resources Edit an existing tileset Save or export the image to the same PNG file registered in the workspace catalog. The studio notices the changed file and updates the palette and map view automatically, usually within one or two seconds. Saving a layered source document alone does not update its exported PNG. Tile positions, tile shapes and Undo history stay intact. Keep the atlas dimensions and sprite positions unchanged when redrawing a tileset. Changing the atlas layout also requires updating its sprite rectangles in the catalog. The default workspace keeps Maps/ and Textures/ beside the launcher or at the source project root. Palette settings and resource mappings are managed in .studio/catalog.json. Open File → Storage folders to locate your maps and images. Existing root catalogs move into .studio automatically, with the original retained in .studio/catalog-backup/. Explicit custom catalog paths are preserved. Each sprite's asset identifies its image, for example Textures/palettes/stage-1/atlas-1.png. Newly added palettes use PNGs in the workspace's Textures/palettes/ directory. Workspace settings may configure another texture root. Edit the registered workspace image; changing a separate copy will not update it. The bundled sample images are fallbacks until a matching workspace image is provided. Checks run in the background and are based on unique image files, independent of painted tile count. Incomplete or locked saves are retried, and the browser retains the previous image while a replacement loads. A file whose timestamp and length were preserved is also checked periodically; these changes can take several additional seconds. Share one source with an engine Use the studio workspace as the shared source folder. Edit each PNG there and configure the engine adapter with that same catalog and resource root. This keeps one editable original; engine-generated assets are derived copies for rendering and packaging. The engine package remains a reusable loader, separate from your project data. Every adapter reads the same three parts: File Contents --- --- Map or room JSON Room layout, tile material IDs, shapes and objects Matching catalog JSON Material IDs, image paths and sprite rectangles Image files The pixels referenced by the catalog For file-based import, select the workspace catalog, exported map/rooms and resource root directly. A shared workspace can look like this: text world/ Maps/ room-01.json .studio/ catalog.json Textures/ palettes/ palette-id.png Choose world/ as the resource root and world/.studio/catalog.json as the catalog in the adapter. Preserve the exact case of paths in the catalog. For a shared file-based workspace, keep the default Textures/ layout so every loader resolves the same originals. To hand the current map to another machine, use File → Export project bundle…. It includes the matching catalog, registered palette sources and referenced images, including bundled fallbacks, at their resource paths. See [Project bundles](project-bundles.md). A map JSON alone does not contain texture pixels or the sprite mapping. Adding or redrawing a tileset does not require rebuilding the engine package. The Unity connection window already downloads catalog textures from the running studio. With Follow web edits enabled, the texture revision also requests a resource refresh. For the other adapters, select the matching exported files and use their import/reload action again. Automatic live synchronization is currently provided by the Unity adapter only. Storage and file dialogs Room JSON is generated automatically under Maps/AutoExport/\u003cmap-folder>/. A complete map saved with File → Save as uses the file you select. On Windows, both the web version and application start the map file dialog in Maps. The web version opens a native Windows dialog through its local server. On other browser platforms, use File → Storage folders → Set file dialog folder once to remember that directory for open/save dialogs. Browser downloads without file picker support use the browser’s download folder. Legacy default data is copied once into the accessible layout, without deleting its original folder. Different files with the same target name stop migration instead of overwriting either copy. Explicit --project workspaces retain their configured paths. For release launchers, --storage-root changes the default portable storage location; --project selects an existing workspace without migration. Choose the folder containing Textures as the engine resource root, .studio/catalog.json inside that folder as the catalog, and a JSON inside Maps/AutoExport as the room. Existing engine packages already resolve image IDs relative to that resource root, so there is no machine-specific path inside the exported data. "},{"title":"Tile palettes and autotiling","href":"tilesets.html","text":" Tile palettes and autotiling Set up 4-tile or 47-tile autotiling, import your PNG tileset, and organize palettes into groups. Neighboring tiles determine which sprite appears as you paint. Open … beside a palette in the left sidebar. The same settings apply to its solid and slope brushes. Palette changes affect every tile using that palette; use + to create an independent palette first when needed. Palette groups Use + Group below the brush controls to add a group. Built-in palettes are organized into Default colors and Default themes; the + beside Palette adds a palette. Click a group title to fold or expand it. Drag a palette above or below another palette to change its order. Drop it on a group title or empty group to move it there. Drag a group by its handle to reorder the groups. Search finds both palette and group names. You can also open a palette’s … settings, choose its Group, and use ↑ / ↓ to set its position before applying. The group’s own … button changes its name or position. The palette creation dialog lets you choose a group too. Organization saves automatically to the workspace catalog and survives reopening. It leaves placed tiles, material IDs, PNG paths, and engine resources unchanged. Cancel in the settings dialog discards pending position changes. Four source tiles Choose 4-tile autotiling, then import a PNG strip or four separate PNGs. Each source tile is 16×16 pixels. To replace one slot, select it and import one 16×16 PNG. Selecting a slot alone never changes a rule. Slot Source orientation --- --- 1 Top edge, with terrain continuing below 2 Top-right outer corner, with terrain continuing left and below 3 Top-left inner corner, with an opening toward the top left 4 Interior fill A 64×16 strip fills these four slots from left to right. Multiple PNGs fill slots in the file selection order. Importing one 16×16 PNG replaces the selected slot. The source-image dropdown selects the file whose path is displayed and whose PNG can be downloaded. Ordinary edges and corners retain the complete source tile, rotated for each direction. More complex connections combine 8×8 quarters to cover all 47 normalized neighbor patterns. Check thin walls, isolated tiles and corners in Connection preview: the four source drawings need compatible quarter boundaries. Open Slope tiles to assign the four diagonal directions separately. Slopes are not inferred from the solid tiles. Unassigned slopes use the default shape. Forty-seven source tiles Choose 47-tile assignment for direct control over every connection. Each slot shows its expected shape until assigned. Load template provides a sheet ordered from the top left, row by row: 47 solid tiles followed by four slopes. Download PNG saves the selected source image. Importing a sheet fills slots automatically in that order; missing slots keep their default appearance. Missing slots use the palette's default color and white edges. Switching the method clears the slot assignments inside the dialog; Cancel keeps the saved palette unchanged. Apply saves the palette and updates its tiles immediately. Default PNGs Textures/default/4-tiles/ contains 64×32 sheets: four solid tiles followed by four slopes. Textures/default/47-tiles/ contains 128×112 sheets: 47 solid tiles followed by four slopes. Both folders include green, blue, gray, orange, yellow and purple PNGs, ready to import in their matching mode. Custom palettes use readable folders such as Textures/palettes/stage-1/, with source-1.png originals and numbered generated atlases. The studio keeps previous source bytes under .studio/texture-backup/ when migrating older generated folder names. Material IDs and map cells stay unchanged. Original images and engine resources Imports copy source PNGs into Textures/palettes/\u003cpalette-name>/ beside the launcher or at the source project root. Open folder in palette settings opens the selected texture's folder. File → Storage folders also provides folder shortcuts and path copying. Where browser file pickers are available, Set file dialog folder remembers the directory for open/save dialogs. The Source image field displays that workspace resource path after saving and reopening the dialog. Edit this copy to receive live updates; the initially selected external file is not watched. The studio builds a generated atlas when you apply settings or save an original image. Incomplete image writes retain the previous atlas until a complete replacement is available. Composition runs on image changes, not on paint gestures. The catalog keeps authoring settings in editorTileset and exposes the resulting sprites through its existing sprites entries. Map JSON and palette IDs remain unchanged. Engine adapters continue reading the generated PNG and catalog; no engine-specific rules are embedded in the map. Keep the catalog and workspace textures together when sharing resources. See [Texture editing and engine resources](textures.md). Default tile view in the map toolbar temporarily displays the plain colored tiles. It changes only the viewport, not exported data or palette assignments. Source images must total at most 8 MiB and 4,194,304 pixels per palette. PNG color types, transparency, standard row filters and interlaced images are supported. Every assigned region must fit a complete 16×16 tile. "},{"title":"Validation","href":"validation.html","text":" Validation Run development checks from a source checkout with Node.js 24+, .NET SDK 10, and Git. All commands below run in the background; none publishes a package. Build and unit checks sh node tools/scripting/build-runtime.mjs node platform/shared/build.mjs dotnet run --project metroidvania-studio/core-tests/MetroidvaniaStudio.Core.Tests.csproj -c Release dotnet run --project metroidvania-studio/server-tests/MetroidvaniaStudio.Server.Tests.csproj -c Release dotnet run --project metroidvania-studio/automation-tests/MetroidvaniaStudio.Automation.Tests.csproj -c Release dotnet run --project metroidvania-studio/scripting-tests/MetroidvaniaStudio.Scripting.Tests.csproj -c Release dotnet run --project metroidvania-studio/server-automation-tests/MetroidvaniaStudio.Server.Automation.Tests.csproj -c Release node --test metroidvania-studio/cli-tests/cli.test.mjs metroidvania-studio/cli-tests/live-server.test.mjs node tools/repository/check-text.mjs The live-server test starts its own disposable workspace. It verifies CLI and MCP edits against the actual server, revision conflicts, the shared writer lock, automatic room exports, and Undo. Headless browser checks Install Playwright in a development environment and make its module available as playwright, or set PLAYWRIGHT_MODULE to the module path. Windows defaults to an installed Edge browser; other systems default to Playwright Chromium. METROIDVANIA_STUDIO_BROWSER_CHANNEL overrides the channel. sh node metroidvania-studio/tests/run-automation.mjs --performance This command creates a disposable workspace, builds temporary web files, starts a server on an available loopback port, and closes its own processes afterward. It never uses the working map workspace. Palette checks also cover four-source and 47-slot assignments, individual PNGs, browser/server pixel equivalence, persistence, and original-image hot reload. Checks cover Lua execution, dry runs, failures, cancellation, exactly-once retry after a lost response, Undo/Redo, and JSON batches. Performance checks cover immediate brush drawing, dense tile caches, and pixel equivalence. Failures retain bounded server diagnostics in the temporary test directory. Local installable package sh npm run pack:local npm run test:package The first command produces builds/npm/metroidvania-studio-1.3.2.tgz. The second installs that local archive in a fresh folder, then exercises CLI, Lua, and MCP. Package validation checks its file inventory, document links, metadata, and retained dependency notices. No post-install download or engine installation is needed. 1.2.0 verification The 1.2.0 checks passed for script-library storage, revision conflicts, input validation, explicit script execution, and searchable documentation over HTTP and from local files. Existing Lua cancellation, recovery, Undo/Redo, room exports, and dense-tile rendering checks also passed. Release downloads were extracted and their command-line launchers exercised with the included runtimes on Windows and Linux. The local npm archive passed fresh-install CLI, Lua, and MCP checks. Native macOS execution has not been verified. 1.1.0 verification Windows checks passed for the existing core (41), existing server (82), editing API (30), Lua runtime (16), atomic web jobs (8), CLI/MCP process tests (14), actual live-server integration (1), and repository/build/package tooling (44). Headless browser tests passed, including lost-response recovery and room export compatibility. Dense brush processing P95 was approximately 0.2–0.3 ms on the development machine; 262,144-tile cache tests retained exact pixels. These figures measure the tested drawing work, not universal end-to-end input latency. A fresh clone also built with empty NuGet and HTTP caches: the interpreter was compiled from its pinned upstream source, all four CLI dependencies were restored, and the web, launcher, server, CLI, and five engine packages built without a game project or engine installation. The package installed from that clone passed its CLI, Lua, and MCP tests. The locally installed npm package passed on Windows and native Linux under WSL. Linux checks also verified termination of a worker when its owning process exits. macOS shell wrappers and portable managed outputs were checked, but native macOS execution still needs verification before claiming a tested macOS 1.1.0 release. Release packaging checks the approved main commit, archive contents, version metadata, licenses, and checksums before publication. See the [publication checklist](distribution/publication.md). "},{"title":"Map JSON format","href":"metroidvania-studio--contracts--format.html","text":" Map JSON format The single source of the document contract is core/workspace/MapDocument.cs and its referenced data classes. contracts/Program.cs reflects compiled types to generate web/contracts.generated.ts. The same build generates contracts/map-format-v2.schema.json; node metroidvania-studio/build-web.mjs --check-contracts checks both outputs for drift. Version and encoding Documents use formatVersion: 2 and tileSize: 16. Saved and exported maps use compact JSON without indentation or a trailing newline. Numeric fields use culture-independent JSON numbers with a decimal point; loaders also accept whitespace-formatted JSON. User string values retain their whitespace and content. Output is UTF-8 without a byte-order mark; an optional UTF-8 byte-order mark is accepted on input. Version 1 files migrate to version 2 with empty layer groups. Future versions, unknown structural fields, duplicate JSON keys and invalid values are rejected before replacing the current document. A missing optional field retains its documented model default. The JSON schema describes structural validation. MapDocumentStore additionally validates unique IDs, group references, room containment, finite coordinates and bounded aggregate work. Each map file and an aggregate room export are limited to 32 MiB. A room can be up to 1024 by 1024 tiles; a document can contain at most 1024 rooms. The software release version is independent of the map format version. Coordinates and identity Room positions are integer world coordinates in tile units. Positive X points right and positive Y points up. Tile cells, object positions and path nodes use coordinates local to their room. Object rectangles use a lower-left origin; rotation is in degrees around the rectangle center. A cell is 16 by 16 source pixels. PPU and reference resolution do not change stored tile coordinates. A consuming engine converts tile coordinates to world units by multiplying by 16 / PPU. Shape values remain stable: 0 is solid, 1 fills the bottom-left triangle, 2 the bottom-right triangle, 3 the top-left triangle and 4 the top-right triangle. Layer values are 0 foreground tiles, 1 background tiles, 2 entities, 3 triggers, 4 foreground decorations and 5 background decorations. The value 6 selects all layers in editing commands and is not a stored content layer. Room and object IDs are stable opaque strings. A tile material or object definition ID is resolved through the selected workspace catalog, never through a machine path or engine object. Existing identifiers retain their spelling. Sprite atlas entries contain a relative texture resource name and a source rectangle. Applications importing JSON provide their own resource resolver; map documents do not embed images or execute object definitions. Import and export A complete authoring file contains rooms, shared properties, background layers and layer groups. Each exported room file uses the same document envelope with one room, preserving relevant shared metadata and stable IDs. This lets a consumer use one reader for complete maps and individual rooms. User-defined object properties and unknown background effect properties are opaque data and survive an import/export round trip. The editor does not run their game behavior. Unknown structural fields require an explicit format update instead of being silently discarded. Any future incompatible change requires a format-version increment and an explicit migrator. Generated declarations and JSON schema, literal version-2 compatibility tests, Unicode round trips and existing format migration tests must pass together before publication. Per-map camera settings Camera settings use reserved document property keys metroidvaniaStudio.camera.ppu, metroidvaniaStudio.camera.width and metroidvaniaStudio.camera.height. Values are decimal integer strings; PPU accepts 1–8192 and each resolution dimension 1–16384. The web settings action writes all three together in one undoable transaction. Reserved values are validated before import or commit. Using the existing metadata envelope preserves format-version-2 compatibility: older readers can retain these properties without knowing their meaning. Missing settings use the workspace catalog defaults (normally 16 / 320 / 180). Explicit settings travel with complete maps and every room export. Camera half-height in world units is height / (2 × PPU); visible source tile extent is width / 16 by height / 16. The server returns an effective camera with each changed state response, including compact edit acknowledgements. A state request with full=true includes both document and resource catalog whenever revision tokens are stale; an unchanged request still returns 204. This permits consumers with inline JSON object serialization to request complete snapshots without polling large unchanged documents. The build also generates engine adapter DTOs, camera-setting logic and tile-mask geometry from the same core definitions. --check-contracts checks the TypeScript, schema and adapter outputs together. Engine API code stays under the corresponding integrations/\u003cengine> source folder. Distributable archives live under engine-packages/\u003cengine>. Trigger event properties Triggers use the existing object id and properties fields. event holds None or Trigger001 through Trigger200; once holds a boolean string; desc holds the description. Runtime enum values are 0 through 200 across every engine package. Portals and invisible walls use their object ID, definition, bounds and desc property. They are independent of numbered events, and runtime event managers ignore their legacy event and once properties. These settings do not change map format 2. Legacy free-text events remain intact and are unassigned at runtime until mapped to a numbered event. "}];
